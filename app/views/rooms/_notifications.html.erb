<% unread = room.notifications_as_room.where(type: MessageNotification.name,
                                            recipient: current_user).unread %>

<% if unread.count > 0 %>
<span class="btn btn-primary"> <%= "#{unread.count} unread messages" %></span>
   
<% end %>